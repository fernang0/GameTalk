# 📐 Arquitectura de la Integración API REST

## 🏗️ Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI LAYER (Compose)                       │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │ TopicsScreen   │  │ CreateTopic    │  │ EditTopic      │   │
│  │                │  │ Screen         │  │ Screen         │   │
│  └────────┬───────┘  └────────┬───────┘  └────────┬───────┘   │
│           │                   │                   │             │
│           └───────────────────┴───────────────────┘             │
│                               │                                 │
└───────────────────────────────┼─────────────────────────────────┘
                                │
                                │ collectAsState()
                                │
┌───────────────────────────────▼─────────────────────────────────┐
│                      VIEWMODEL LAYER                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            ApiTopicViewModel                             │   │
│  │  ┌──────────────────────────────────────────────┐       │   │
│  │  │  StateFlow<Resource<List<TopicDTO>>>          │       │   │
│  │  │  - topicsState                                │       │   │
│  │  │  - topicState                                 │       │   │
│  │  │  - operationState                             │       │   │
│  │  └──────────────────────────────────────────────┘       │   │
│  │                                                           │   │
│  │  Methods:                                                │   │
│  │  - getAllTopics()                                        │   │
│  │  - getTopicsByCategory(categoryId)                       │   │
│  │  - createTopic(...)                                      │   │
│  │  - updateTopic(...)                                      │   │
│  │  - deleteTopic(topicId)                                  │   │
│  └─────────────────────┬───────────────────────────────────┘   │
│                        │                                         │
│  ┌─────────────────────▼───────────────────────────────────┐   │
│  │            ApiUserViewModel                              │   │
│  │  StateFlow<Resource<UserDTO>>                            │   │
│  │  - createUser(email, password, username)                 │   │
│  │  - getUserById(userId)                                   │   │
│  │  - changePassword(userId, newPassword)                   │   │
│  └─────────────────────┬───────────────────────────────────┘   │
└────────────────────────┼─────────────────────────────────────────┘
                         │
                         │ viewModelScope.launch
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                    REPOSITORY LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   ApiRepository                           │   │
│  │  ┌────────────────────────────────────────────────┐      │   │
│  │  │  Flow<Resource<T>>                              │      │   │
│  │  │  - Loading → API Call → Success/Error          │      │   │
│  │  └────────────────────────────────────────────────┘      │   │
│  │                                                            │   │
│  │  User Operations:                                         │   │
│  │  ├─ getAllUsers()                                         │   │
│  │  ├─ getUserById(id)                                       │   │
│  │  ├─ createUser(email, password, username)                │   │
│  │  └─ changePassword(userId, newPassword)                  │   │
│  │                                                            │   │
│  │  Topic Operations:                                        │   │
│  │  ├─ getAllTopics()                                        │   │
│  │  ├─ getTopicsByCategory(categoryId)                      │   │
│  │  ├─ getTopicsByUser(userId)                              │   │
│  │  ├─ getTopicById(id)                                     │   │
│  │  ├─ createTopic(categoryId, userId, title, desc)        │   │
│  │  ├─ updateTopic(topicId, categoryId, userId, ...)       │   │
│  │  └─ deleteTopic(topicId)                                 │   │
│  │                                                            │   │
│  │  Error Handling:                                          │   │
│  │  ├─ HTTP errors (400, 401, 404, 500, etc.)              │   │
│  │  ├─ Network errors (timeout, no connection)              │   │
│  │  └─ Custom error messages                                │   │
│  └────────────────────┬───────────────────────────────────┘   │
└─────────────────────────┼────────────────────────────────────────┘
                          │
                          │ suspend functions
                          │
┌─────────────────────────▼────────────────────────────────────────┐
│                     NETWORK LAYER                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                 RetrofitClient                            │   │
│  │  ┌────────────────────────────────────────────────┐      │   │
│  │  │  Singleton Instance                             │      │   │
│  │  │  - BASE_URL: http://10.0.2.2:8080/            │      │   │
│  │  │  - OkHttpClient (logging, timeouts)            │      │   │
│  │  │  - GsonConverterFactory                         │      │   │
│  │  └────────────────────────────────────────────────┘      │   │
│  └────────────────────┬───────────────────────────────────┘   │
│                       │                                          │
│  ┌────────────────────▼───────────────────────────────────┐   │
│  │                 ApiService                              │   │
│  │  ┌──────────────────────────────────────────────┐      │   │
│  │  │  Retrofit Interface                           │      │   │
│  │  │                                                │      │   │
│  │  │  @GET("api/users")                            │      │   │
│  │  │  @GET("api/users/{id}")                       │      │   │
│  │  │  @POST("api/users")                           │      │   │
│  │  │  @PATCH("api/users/{id}/password")           │      │   │
│  │  │                                                │      │   │
│  │  │  @GET("api/topics")                           │      │   │
│  │  │  @GET("api/topics?categoryId={id}")          │      │   │
│  │  │  @GET("api/topics?userId={id}")              │      │   │
│  │  │  @GET("api/topics/{id}")                      │      │   │
│  │  │  @POST("api/topics")                          │      │   │
│  │  │  @PUT("api/topics/{id}")                      │      │   │
│  │  │  @DELETE("api/topics/{id}")                   │      │   │
│  │  └──────────────────────────────────────────────┘      │   │
│  └────────────────────┬───────────────────────────────────┘   │
└─────────────────────────┼────────────────────────────────────────┘
                          │
                          │ HTTP Requests
                          │
┌─────────────────────────▼────────────────────────────────────────┐
│                    SPRING BOOT API                                │
│                   http://[SERVER]:8080                            │
│                                                                   │
│  Endpoints:                                                       │
│  ├─ GET    /api/users                                            │
│  ├─ GET    /api/users/{id}                                       │
│  ├─ POST   /api/users                                            │
│  ├─ PATCH  /api/users/{id}/password                             │
│  │                                                                │
│  ├─ GET    /api/topics                                           │
│  ├─ GET    /api/topics?categoryId={id}                          │
│  ├─ GET    /api/topics?userId={id}                              │
│  ├─ GET    /api/topics/{id}                                      │
│  ├─ POST   /api/topics                                           │
│  ├─ PUT    /api/topics/{id}                                      │
│  └─ DELETE /api/topics/{id}                                      │
│                                                                   │
│  Database: PostgreSQL / MySQL / H2                               │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos

### **1. Usuario interactúa con UI**
```
User taps button → Composable calls ViewModel method
```

### **2. ViewModel procesa**
```
ViewModel.getAllTopics() 
  ↓
viewModelScope.launch {
  repository.getAllTopics().collect { resource ->
    _topicsState.value = resource
  }
}
```

### **3. Repository ejecuta**
```
Repository.getAllTopics()
  ↓
emit(Resource.Loading())
  ↓
try {
  val response = apiService.getAllTopics()
  emit(handleResponse(response))
} catch (e: Exception) {
  emit(Resource.Error(e.message))
}
```

### **4. Retrofit llama a la API**
```
ApiService.getAllTopics()
  ↓
Retrofit creates HTTP request
  ↓
OkHttp sends request to server
  ↓
Server responds with JSON
  ↓
Gson converts JSON to TopicDTO
  ↓
Response<List<TopicDTO>> returned
```

### **5. Repository procesa respuesta**
```
Response<List<TopicDTO>>
  ↓
if (response.isSuccessful) {
  Resource.Success(response.body())
} else {
  Resource.Error(error message)
}
```

### **6. ViewModel actualiza estado**
```
_topicsState.value = Resource.Success(topics)
  ↓
StateFlow emits new value
```

### **7. Composable reacciona**
```
val topicsState by viewModel.topicsState.collectAsState()
  ↓
Recomposition triggered
  ↓
UI updates with new data
```

---

## 🎯 Estados del Resource

```
┌─────────────────────────────────────────────────────────┐
│              Resource<T> Sealed Class                    │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Loading    │  │   Success    │  │    Error     │  │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤  │
│  │ data: null   │  │ data: T      │  │ data: null   │  │
│  │ message: -   │  │ message: -   │  │ message: str │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                           │
│  UI Behavior:                                            │
│  - Loading: Show CircularProgressIndicator              │
│  - Success: Show data in LazyColumn                      │
│  - Error: Show error message + retry button              │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Modelos de Datos

### **UserDTO**
```
┌─────────────────────────┐
│       UserDTO           │
├─────────────────────────┤
│ id: Int                 │
│ email: String           │
│ username: String        │
└─────────────────────────┘
         ▲
         │
         │ Created by
         │
┌────────┴─────────────────┐
│   UserCreateDTO          │
├──────────────────────────┤
│ email: String            │
│ password: String         │
│ username: String         │
└──────────────────────────┘
```

### **TopicDTO**
```
┌──────────────────────────────┐
│         TopicDTO             │
├──────────────────────────────┤
│ id: Int                      │
│ categoryId: Int              │
│ userId: Int                  │
│ title: String                │
│ description: String          │
│ createdAt: Long              │
│ repliesCount: Int            │
│ viewsCount: Int              │
│ lastActivity: Long           │
│ categoryName: String         │
│ username: String             │
└──────────────────────────────┘
         ▲
         │
         │ Created/Updated by
         │
┌────────┴──────────────────────┐
│   TopicCreateDTO              │
├───────────────────────────────┤
│ categoryId: Int               │
│ userId: Int                   │
│ title: String                 │
│ description: String           │
└───────────────────────────────┘
```

---

## 🔐 Manejo de Errores

```
┌────────────────────────────────────────────────────────┐
│                 Error Handling Flow                     │
│                                                          │
│  API Call                                                │
│     ↓                                                    │
│  try {                                                   │
│    val response = apiService.call()                     │
│    if (response.isSuccessful) {                         │
│      ✅ Resource.Success(data)                          │
│    } else {                                              │
│      ❌ Resource.Error("HTTP ${response.code()}")      │
│    }                                                     │
│  } catch (e: Exception) {                               │
│    ❌ Resource.Error(e.message)                         │
│  }                                                       │
│                                                          │
│  Error Types:                                            │
│  ├─ 400 → "Solicitud incorrecta"                       │
│  ├─ 401 → "No autorizado"                              │
│  ├─ 404 → "No encontrado"                              │
│  ├─ 500 → "Error interno del servidor"                 │
│  ├─ Timeout → "Tiempo de espera agotado"               │
│  ├─ No connection → "No se pudo conectar"              │
│  └─ Unknown → "Error: {message}"                        │
└────────────────────────────────────────────────────────┘
```

---

## 🚀 Ejemplo Completo de Flujo

### **Crear un Topic**

```
┌─────────────────────────────────────────────────────────────┐
│  1. USER ACTION                                              │
│     User fills form and taps "Crear Topic" button          │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  2. UI LAYER (Compose)                                       │
│     CreateTopicScreen                                        │
│     ↓                                                         │
│     viewModel.createTopic(categoryId, userId, title, desc)  │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  3. VIEWMODEL                                                │
│     ApiTopicViewModel.createTopic()                          │
│     ↓                                                         │
│     viewModelScope.launch {                                  │
│       repository.createTopic(...).collect { resource ->     │
│         _operationState.value = resource                     │
│       }                                                       │
│     }                                                         │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  4. REPOSITORY                                               │
│     ApiRepository.createTopic()                              │
│     ↓                                                         │
│     emit(Resource.Loading())                                 │
│     ↓                                                         │
│     val response = apiService.createTopic(dto)              │
│     ↓                                                         │
│     emit(handleResponse(response))                           │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  5. NETWORK LAYER                                            │
│     ApiService.createTopic()                                 │
│     ↓                                                         │
│     @POST("api/topics")                                      │
│     suspend fun createTopic(@Body topic: TopicCreateDTO)    │
│     ↓                                                         │
│     HTTP POST → http://10.0.2.2:8080/api/topics            │
│     Body: { categoryId, userId, title, description }        │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  6. SPRING BOOT API                                          │
│     TopicController.createTopic()                            │
│     ↓                                                         │
│     Save to database                                         │
│     ↓                                                         │
│     Return TopicDTO with ID                                  │
│     ↓                                                         │
│     HTTP 200 + JSON body                                     │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  7. NETWORK LAYER                                            │
│     Retrofit receives response                               │
│     ↓                                                         │
│     Gson converts JSON → TopicDTO                           │
│     ↓                                                         │
│     Response<TopicDTO> returned                              │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  8. REPOSITORY                                               │
│     handleResponse(response)                                 │
│     ↓                                                         │
│     if (isSuccessful) Resource.Success(topicDTO)            │
│     ↓                                                         │
│     emit(Resource.Success(topicDTO))                         │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  9. VIEWMODEL                                                │
│     _operationState.value = Resource.Success(topicDTO)      │
│     ↓                                                         │
│     StateFlow emits new state                                │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  10. UI LAYER (Compose)                                      │
│      val operationState by viewModel.operationState          │
│        .collectAsState()                                     │
│      ↓                                                        │
│      LaunchedEffect(operationState) {                        │
│        when (operationState) {                               │
│          is Success → onTopicCreated()                       │
│        }                                                      │
│      }                                                        │
│      ↓                                                        │
│      Navigate back or show success message                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Comparación con Arquitectura Anterior

### **ANTES (Room)**
```
UI → ViewModel → Repository → DAO → Room → SQLite
                   ↑___________________________|
                   (Flow from database)
```

### **AHORA (API REST)**
```
UI → ViewModel → Repository → Retrofit → Spring Boot API
                   ↑___________________________|
                   (Flow with Resource states)
```

### **FUTURO (Híbrido)**
```
                  ┌─→ Room (cache) ─┐
UI → ViewModel → Repository          → Combine → StateFlow
                  └─→ API (fresh) ───┘
```

---

## 🎓 Ventajas de esta Arquitectura

### ✅ **Separación de Responsabilidades**
- UI solo renderiza
- ViewModel solo gestiona estado
- Repository solo maneja datos
- Network solo hace llamadas HTTP

### ✅ **Testeable**
- Cada capa se puede testear independientemente
- Mock fácil de Repository
- UI tests sin red

### ✅ **Escalable**
- Fácil agregar nuevos endpoints
- Fácil agregar nuevas features
- Fácil cambiar implementación

### ✅ **Mantenible**
- Código organizado
- Responsabilidades claras
- Fácil de entender

### ✅ **Reactive**
- UI se actualiza automáticamente
- No callbacks anidados
- StateFlow maneja el estado

---

**🏗️ Arquitectura completa implementada y lista para usar!**
